apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-demo
  labels:
    app: ping-demo
spec:
  replicas: 6
  selector:
    matchLabels:
      app: ping-demo
  template:
    metadata:
      labels:
        app: ping-demo
    spec:
      containers:
        - name: demoping
          image: ping-demo:4d64221
          ports:
            - containerPort: 20400
