apiVersion: v1
kind: Service
metadata:
  name: ping-demo-service
  labels:
    app: ping-demo
spec:
  selector:
    app: ping-demo
  ports:
    - protocol: TCP
      port: 20400
      targetPort: 20400
  type: NodePort



