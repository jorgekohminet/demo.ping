apiVersion: v1
kind: Service
metadata:
  name: ping-prueba-service
  labels:
    app: ping-prueba
spec:
  selector:
    app: ping-prueba
  ports:
    - protocol: TCP
      port: 20400
      targetPort: 20400
  type: LoadBalancer



